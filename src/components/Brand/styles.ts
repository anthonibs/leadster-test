import { styled } from 'styled-components';

export const Logo = styled.figure`
  height: auto;
  width: 100%;

  user-select: none;
  pointer-events: none;
`;

export const Image = styled.img`
  display: block;

  width: 100%;
  height: 100%;
`;
